<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Articles</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

  <div class="container my-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h1 class="mb-0">Articles Feed 📰</h1>
      <a href="/articles/create" class="btn btn-success">+ Create New Article</a>
    </div>

    <div class="list-group">
      <% for (let i = 0; i < articles.length; i++) { %>
        <a href="/articles/<%= articles[i].id %>" class="list-group-item list-group-item-action">
          <div class="d-flex w-100 justify-content-between">
            <h5 class="mb-1"><%= articles[i].title %></h5>
            <small class="text-muted">ID: <%= articles[i].id %></small>
          </div>
          <p class="mb-1"><%= articles[i].content.length > 100 ? articles[i].content.substring(0, 100) + '...' : articles[i].content %></p>
          <small class="text-muted">Click to read more</small>
        </a>
      <% } %>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
